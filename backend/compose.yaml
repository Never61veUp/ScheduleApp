services:
  scheduleapp.api:
    image: scheduleapp.api
    build:
      context: .
      dockerfile: ScheduleApp.API/Dockerfile
    ports:
      - "8080:8080"
    environment:
      DP_KEYS_FOLDER: "/keys"
      TELEGRAM_BOT_TOKEN: "${TELEGRAM_BOT_TOKEN}"
      JWT_KEY: "${JWT_KEY}"
      JWT_ISSUER: "MiniApp"
      JWT_AUDIENCE: "MiniApp"
    volumes:
      - scheduleapp-keys:/keys
  client:
    build:
      context: ../miniapp
      dockerfile: Dockerfile
    depends_on:
      - scheduleapp.api
    ports:
      - "5173:80"

volumes:
  scheduleapp-keys: